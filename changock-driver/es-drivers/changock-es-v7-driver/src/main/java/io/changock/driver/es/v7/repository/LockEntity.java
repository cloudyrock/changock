package io.changock.driver.es.v7.repository;

public class LockEntity {
  //TODO I don't know what will be used as persistence object. Mongo module uses Document, but I don't think
  // in this case we have something similar, so I guess you need to have a different entity for both, lock and changeEntry.
}
