package io.changock.driver.es.v7.repository.lock;

public class LockEntity {
  //TODO I don't know what will be used as persistence object. Mongo module uses Document, but I don't think
  // we have something similar in ES, so I guess you need to have a different entity for both, lock and changeEntry.
}
